version: 1
applications:
  - appRoot: app-one
    frontend:
      artifacts:
        baseDirectory: out
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
  - appRoot: app-two
    frontend:
      artifacts:
        baseDirectory: out
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
